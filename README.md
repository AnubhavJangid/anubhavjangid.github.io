# anubhavjangid.github.io

Personal portfolio — **Anubhav Jangid (Senior Android Developer)**

## 📦 What’s inside
- `index.html` — Single-page, responsive site (dark/light aware)
- `assets/Anubhav_Jangid_resume.pdf` — Resume
- `assets/favicon.png`, `assets/og-image.jpg` — Branding assets (optional)

## 🚀 Deploy on GitHub Pages
1. Create a new **public** repository named **`anubhavjangid.github.io`** (must match your GitHub username).
2. Upload `index.html` to the repo root.
3. Create an `assets/` folder and upload your resumes:
   - `Anubhav_Jangid_resume.pdf`
4. (Optional) Add `assets/favicon.png` and `assets/og-image.jpg` or remove those `<link/meta>` tags from `index.html`.
5. Go to **Settings → Pages** → **Build and deployment** → **Source: Deploy from a branch** → `main` / root.
6. Your site will be live at **https://anubhavjangid.github.io**.

## 🔧 Customize
- Update links (LinkedIn, GitHub, StackOverflow) inside `index.html`.
- Replace the two resume PDFs in `/assets` when you update them later.
- Swap `assets/og-image.jpg` and `assets/favicon.png` with your own branding (or remove those references).

## 🧪 Local preview
Just open `index.html` in your browser. No build tools needed.

